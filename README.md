# property_manager_app
Property Manager web app

## Installation
Update Symfony with "composer update", can use script ./update_symfony.sh which creates a large swap space for VPSes with little memory:
```
./update_symfony.sh
```
Create a .env file from the template:
```
cp .env.dist .env
```
